# 深度视频转换器 - 依赖包
# 核心依赖
torch>=2.1.0
torchvision>=0.12.0
opencv-python>=4.5.0
pillow>=8.0.0
numpy>=1.21.0
tqdm>=4.62.0

# AI模型依赖
transformers>=4.20.0
accelerate>=0.20.0

# 颜色深度功能依赖
scikit-learn>=1.0.0

# Web界面依赖
gradio>=3.0.0

# 视频处理依赖
ffmpeg-python>=0.2.0

# 可选依赖（用于更好的性能）
# 注意：这些需要额外的配置才能使用
# tensorrt  # NVIDIA GPU加速（需要TensorRT SDK）
# onnxruntime-gpu  # ONNX GPU推理（需要CUDA）
# torch-audio  # 音频处理加速
# torchaudio  # 音频处理库
